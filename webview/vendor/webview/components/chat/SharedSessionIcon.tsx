import { UiIcon } from '../UiIcon';
import { shareAndroidIcon } from '../../lib/ui-icons';

export function SharedSessionIcon() {
  return <UiIcon source={shareAndroidIcon} class="shared-session-icon" width={12} height={12} />;
}
